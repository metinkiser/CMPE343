package CinemaCenter;

public class SelectedSession {
    private static Session session;

    public static void setSession(Session selectedSession) {
        session = selectedSession;
    }

    public static Session getSession() {
        return session;
    }
}
